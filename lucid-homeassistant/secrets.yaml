# MQTT credentials for Home Assistant to connect to EMQX.
# Must match the password set when running:
#   python lucid-infra/lucid-auth/manage.py add-ha-bridge ha-bridge --password <password>
# Set HA_BRIDGE_MQTT_PASSWORD in your .env to the same value.
mqtt_username: ha-bridge
mqtt_password: changeme
